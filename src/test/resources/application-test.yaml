spring:
  datasource:
    url: jdbc:postgresql://${DB_URL:localhost:54322/wishlist_test}
    username: ${DB_USERNAME:fur}
    password: ${DB_PASSWORD:lucis}
    driverClassName: org.postgresql.Driver
  liquibase:
    drop-first: true